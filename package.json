{
  "name": "autopost-full-app",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend & npm run dev --workspace=frontend",
    "start": "npm start --workspace=backend",
    "build": "npm run build --workspace=frontend"
  }
}
